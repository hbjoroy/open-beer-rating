apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-tappd.fullname" . }}
  labels:
    {{- include "open-tappd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-tappd.selectorLabels" . | nindent 4 }}
